# Contributing / 贡献

- Fork and create feature branches / Fork 仓库并创建功能分支
- Run extension tests in `web-extension/tests/test_core.html` / 运行浏览器端测试
- For Android, build `simplay-core` with Gradle and run unit tests / 构建并运行单元测试
- Open PRs with a clear description and checklist / 提交包含说明与检查清单的 PR
